---
description: 主线预检、快修通道、progress.json 裁剪
alwaysApply: true
---

# 工作流：预检、快修与 progress 裁剪

## 主线：先预检再实现

当用户提出需求时，先做「可行性/材料完备性」检查，再开始修改。

### 必做（开始改动前）
- 把需求翻译成**可验证前置条件**（字段、粒度、口径、日期范围/覆盖、输入输出路径）。
- 跑最小自检/快速证据（读取关键文件、检查日期覆盖、检查缺字段/缺 sheet/缺列）。

### 若前置条件不满足
- **先停下并反馈**，不要继续硬改。
- 反馈须含：缺口与证据；影响范围（哪些图表/指标会空或错）；选项 **A 补材料** / **B 降级**（说明牺牲与输出形态）。

### 开始实现的条件
- 自检通过，或用户明确选择降级并接受影响。

### progress.json（`/save` 或「本次保存已完成」）
写入 `recent_completed` 后该数组**最多 3 条、最新在前**；超过则**删最旧**。旧内容以 task card 为准，不必迁回 progress。

---

## 快修（Hotfix Express）

当且仅当**同时**满足：单文件或单函数、≤3 轮可完成、只需目标文件上下文、不改变业务结论/不新增模块/不变更数据口径 → 进入快修。

### 快修下跳过
- 方案讨论（主线里的选项与优劣展开）
- 新建 task card、更新 progress.json、候选发布双角色评审

### 快修下执行
1. 读目标文件，改，验证（py_compile / lint 等视类型而定）
2. 向用户给 diff 摘要
3. 用户确认后自动 commit：`hotfix: <一句话中文描述>`

### 安全阀
- 用户说「先别提交 / 不要 commit」→ 跳过自动 commit
- 范围超预期（跨文件、口径变更等）→ **退出快修**，改走上文主线流程

### 不符合快修时
- 简短说明超出快修范围，按**主线**执行。
