---
description: 统一项目产出目录结构（data/code/result），便于版本控制与引用
alwaysApply: true
---

# 项目产出组织规范（全局）

## 标准目录结构

- 每个独立项目（或一个分析/需求/交付）在其根目录下必须包含：
  - `data/`：所有输入数据与中间数据（可再分 `raw/`、`intermediate/`、`external/`）
  - `code/`：所有脚本、SQL、notebook、配置与可复现代码
  - `result/`：所有输出结果（图表、报表、导出 CSV、模型文件、最终交付物）

## 文件放置规则

- 任何“新生成/新导出”的文件必须落在 `data/` 或 `result/`，不得散落在项目根目录。
- 任何“可复现逻辑”必须落在 `code/`，不得把逻辑写进 `result/` 或 `data/`。
- `result/` 仅存放产物与可阅读输出；可选增加 `result/figures/`、`result/tables/`、`result/reports/`。

## 命名与可引用性

- 目录与文件名使用小写英文、数字与下划线：`snake_case`；避免空格与中文路径，保证跨平台引用稳定。
- 输出文件名需可追踪来源：包含日期或版本号（例如 `2026-03-17_...` 或 `v001_...`）。

## 版本控制建议

- 对于体积大或可再生成的文件：优先忽略（如 `data/raw/`、`result/` 下的临时产物），保留生成脚本与最小必要样例数据。
- 在 `code/` 内提供“一键复现”的入口脚本或说明（如 `code/run.py`、`code/README.md`）。
